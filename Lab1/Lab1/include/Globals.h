#pragma once
#include <SFML/Graphics.hpp>

#define SCREEN_WIDTH sf::VideoMode::getDesktopMode().width / 4 * 3
#define SCREEN_HEIGHT sf::VideoMode::getDesktopMode().height / 4 * 3

#define DEBUG_WIDTH SCREEN_WIDTH / 4